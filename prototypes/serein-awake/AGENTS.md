# Prototype Instructions

Run the local server yourself and open the preview in the browser available to this environment. Do not give the user server-start instructions when you can run it.

Before making substantial visual changes, use the Product Design plugin's `get-context` skill when the visual source is unclear or no longer matches the current goal. When the user gives durable prototype-specific design feedback, preferences, or decisions, record them in `AGENTS.md`.

When implementing from a selected generated mock, treat that image as the source of truth for layout, component anatomy, density, spacing, color, typography, visible content, and hierarchy.

Build app UI in `src/`. Keep `.openai/hosting.json`, `worker/index.js`, `scripts/prepare-sites-build.mjs`, and `tests/sites-worker.test.mjs` intact so the same local prototype can be handed to Sites. Before a Sites handoff, run `npm run build` and `npm run test:sites`; the build must leave `dist/client/index.html`, `dist/server/index.js`, and `dist/.openai/hosting.json`.

## Prototype direction

- Preserve the approved `醒来` cover and white-content states while extending the other primary areas one page at a time.
- The dedicated Window Shadow reading surface belongs to `醒来`; keep the small `上一窗影` section as its latest-Shadow preview rather than mistaking that summary for the complete surface.
- Open the Window Shadow reader from the `上一窗影` section on `醒来`; do not add Window Shadow as a new primary sidebar area. Use a near-full-viewport paper overlay with the Awake page softened beneath it, a chronological Shadow index on desktop, and a read-only detail pane that defaults to the latest Shadow. On narrow screens, enter through the Shadow list, then open one full-screen detail with a clear return to the list. Closing the reader returns focus and scroll context to `醒来`.
- Keep prototype Window Shadows in a separate frontend data module so a canonical API can replace them. Real wiring must project canonical Shadow text unchanged; never infer or generate a Shadow body from Scene titles, summaries, or relation metadata.
- For the current local rehearsal, load Window Shadows from the read-only local Vite bridge to the live Ombre `window_shadows.sqlite`, with the ignored `public/private/window-shadows-snapshot.json` only as an offline fallback. The 2026-07-18 Germany Haven Bridge handoff has since been admitted to Ombre as `window_8a89aaaafc1d106005622afb`; show that canonical historical Shadow once, labeled `已入库窗影`, while preserving the original Germany Markdown outside the UI projection.
- Add a batch-selection mode to the Memory surface with bulk archive and bulk delete actions. Keep destructive deletion distinct from reversible archive/restore, preserve the active filters while selecting, and require explicit confirmation before deleting selected Scenes.
- Canonical batch deletion must reuse Serein's Scene tombstone transaction so the Scene search document, Scene/cue FTS rows, Scene/cue embeddings, and endpoint relations are invalidated together. Archiving/`已沉底` is reversible presentation lifecycle and must not delete embeddings.
- The opening state is a relationship cover: a rainy photograph that naturally fades into white, two user-supplied circular avatars with hairline borders, names, short portraits, and time together.
- Wheel or trackpad movement drives a pinned full-screen white-out transition, not an ordinary downward page scroll.
- The detailed portrait, previous Window Shadow, and latest Dream preview appear only after the page has turned white.
- Keep the five primary areas. On the white `醒来` surface, pair `上一窗影` with `梦境`; the Dream preview opens a near-full-viewport read-only archive rather than adding a sixth sidebar destination. Use the latest retained dream and show its real date rather than calling an older retained dream `昨夜`. Load Dream metadata and retained bodies through the authenticated local bridge to Ombre `/api/dreams` and `/api/dreams/{dream_id}` without inventing titles or body text.
- Preserve `还在想的事` only inside the authored Window Shadow document and its reader. Do not call it `线头`, turn it into a task list, or expose it as ordinary recall; the Awake summary slot belongs to Dreams.
- The vertical sidebar is hidden in the cover state, appears with the white content state, stays collapsed, and expands as an overlay on hover.
- Use the two images in `C:\Users\86188\Desktop\头像` as the avatars. Do not replace them with generated portraits.
- Keep cover, avatar, and name replacement in a future settings page. The cover itself has no editing controls; only the detailed portrait avatar keeps its edit affordance.
- The photograph ends around the upper half of the desktop viewport and fades to white before the avatar centers. The two cover avatars sit slightly higher and closer together than the first pass.
- Use one restrained serif language for both names. Source Serif 4 plus Noto Serif SC is the open, self-hosted prototype substitute for the Tiempos Text direction.
- Keep both the relationship-day line and the sentence below the avatars editable from Settings. Let the user adjust where the cover begins fading to white, and persist those choices locally.
- Keep a subtle white radial frosted layer behind the paired avatars. Its visibility belongs to the cover Settings, defaults on, persists locally, and stays independent from the black-and-white composition toggle.
- `记忆` is a Scene-only browsing surface. Do not add a separate `碎片记忆` category because Scene already owns that role.
- Build `记忆` as a searchable, filterable vertical timeline. Borrow the reference's time-ordered structure, but keep this prototype's monochrome editorial language and existing hover-expanded global sidebar.
- Keep Scene/source evidence visibly distinct. Expanding a Scene may reveal a source excerpt, while source text remains the evidence layer rather than becoming decorative metadata.
- Keep the global navigation and memory filters still. The independently scrolling surface is the Scene timeline on the right.
- `舍不得丢的` is the favorite filter label, with an outline heart in the filter rail.
- Scene lifecycle views are `已沉底` (archived) and `可浮现` (recallable). Do not keep compatibility labels for `已沉淀` or `已确认`.
- Hide the entire `被叙事卷引用` group when a Scene has no narrative-roll references.
- Build `叙事卷` as a quiet row of front-facing paper volumes. Adapt only the restrained cover-and-page motion from `D:\APPs\CSS-Book-Effect`; do not copy its palette, iconography, or documentation-card styling.
- Use the five reviewed local Narrative Rolls and keep their data in a separate frontend data module so a real registry/API can replace the mock data later.
- Keep the bookshelf architectural and paper-like: cool white surfaces, hairline rules, small differences in spine height and width, and no literal wooden furniture or heavy Three.js scene.
- Preserve room for future growth with one quiet, dashed, unnamed spine. It represents a roll that has not formed yet and is not a creation button.
- On desktop, hovering or keyboard-focusing a volume lifts it only slightly and opens the cover enough to reveal the page layers. It must stay inside its own slot, never expand into a preview card or cover a neighboring roll.
- Clicking a roll opens a sidebar-free, full-viewport article reader. Hide the reader scrollbar visually while preserving wheel, trackpad, touch, and keyboard scrolling.
- Use whitespace rather than repeated horizontal rules in the full reader. The reading toolbar and article header do not need divider lines.
- Treat `docs/2026-07-20 Narrative Projection arc样卷-归航.md` as the current `归航` source contract, but keep the reading surface literary: render only the first-person narrative paragraphs and the collapsible source ledger. Do not show “第一人称叙事”, interpretation/current-state notes, candidate-selection notes, generation method, or publication boundary in the reader.
- Treat `## Canonical Scene 来源账` as the same formal source-ledger contract as `## 来源账` and `## Scene 来源账`. Render every listed canonical Scene rather than falling back to cover-summary Scene tags; when that ledger omits dates, use the same document's explicit reconstructed-time ledger instead of guessing from migrated Scene dates. Display Narrative Roll IV as `借别人的故事说自己` in the prototype so its cover title stays within the book face; this is a presentation title and must not silently revise the live Narrative Roll document.
- Keep narrative Chinese typography in the existing Noto Serif SC editorial family. Borrow the Gemini narrative reference's narrow reading measure, centered hierarchy, and generous line height without importing Inter or changing the global type system.
- On narrow screens, keep all rolls reachable in a horizontally scrolling row of book covers whose scrollbar stays hidden, then use the same full-viewport single-column reader.
- Keep `日记` quieter than `记忆`: the default desktop surface is a compact diary index beside one long-form reading page, with search, write, comments, and Darkroom as the only primary actions.
- Keep month-calendar browsing as a secondary toggle inside the diary index. It should help locate written days without permanently taking half the reading surface.
- Use the approved editorial serif rhythm and small outlined quote chips for Diary references. Diary remains authored long-form source text; references to Scene or Narrative Roll are visible relations, not a conversion of the diary into memory.
- New Diary entries and frontend comments may persist in local browser storage for prototype interaction. Both snapshot the configured Xiaoyu display name from Settings and use the `user` role.
- Diary entries preserve their own author and role. When a calendar date contains multiple entries, open one record and expose previous/next navigation inside the reader; navigation stays within that date, and the calendar does not grow a miniature second index.
- Editing a Diary Entry may change its date, title, excerpt, and body, but must preserve its id, author, role, original time, comments, and references. Keep a local revision increment so the prototype can map cleanly to the real revise API later.
- Darkroom uses the established `unlock_at` boundary. Before that time, show the locked door state and reveal no content; do not add a password bypass.
- Reuse the locked-Darkroom UI and exact reveal sequence from `C:\Users\86188\Desktop\gemini-code-1784644913221.html`: line 1 at 0.5s, line 2 at 2s, line 3 at 4.5s, then line 4 plus the live countdown and actions at 6.5s. Omit the door-crack light and the second action; the only visible button is `在外面等`, and it returns directly to Diary. After `unlock_at`, show only Diary entries marked as Darkroom content; keep them out of the ordinary Diary index, calendar, and search.
- Selecting a Scene opens a right-side detail sheet instead of expanding the row inline. Use a restrained slide-and-veil transition based on `0.8s cubic-bezier(0.22, 1, 0.36, 1)`, and honor reduced motion.
- Scene comments/annotates are called `记忆注脚` in the interface. Do not use the abstract label `原文里的光`.
- When a Scene detail sheet is open, soften the timeline beneath it with a light white veil and about 4 pixels of blur. Keep the filter rail readable and keep all horizontal overflow clipped.
- In this frontend prototype, Scene title, timeline summary, body, favorite state, and annotations are editable and persist in local browser storage. Treat that as prototype interaction, not as a claim about canonical backend mutation semantics.
- Annotation authors come from the annotation data. New annotations created in the frontend default to `Rain` with the `user` role, and annotations can be deleted directly.
- Keep `关联 Scene` and `被叙事卷引用` as separate reference groups in the Scene detail sheet. Do not render the provisional `原始来源` group yet.
- Populate `关联 Scene` only from reviewed, currently valid Scene edges after active-state, content-hash, and verbatim-evidence validation. Group multiple typed edges by the related Scene; do not present embedding neighbors or inferred title similarity as formal associations.
- Treat Markdown as a read-only projection for imported Ombre Scenes and Diary text. Render headings, lists, quotes, links, code, tables, and other common Markdown without executing embedded HTML; editing must preserve the stored source text rather than writing rendered HTML back.
- Keep `自我` as a quiet view in the existing left filter rail, populated only by the explicit Ombre `self_anchor` marker (preserved as `migration_source_self_anchor` during the current rehearsal import), never by folder, title, or prose inference. Keep exact-date filtering beside the existing relative time views.
- Build the Basement first around two tools: `召回模拟` and `例句维护`. Keep them in a quiet vertical tool index rather than a horizontal tab row.
- `召回模拟` must send the original sentence through the same production Gateway full-recall pipeline and configured Qwen query embedding used online. Show the semantic route, applied recall/no-recall action, confidence, competing route scores, candidate count, admission/suppression reasons, and final injected cards. A simulation must not create an official Gateway injection record or forward to the chat model.
- Recall Simulation may save an explicit human training label after a result. Keep these labels separate from real Hook/Gateway observations, mark their source as `manual_simulation`, group them by the active simulation batch, and merge them only in the training export. Save expected recall/skip, optional expected Route and target memory IDs, plus the actually observed action/Route; never store injected bodies or pretend the simulation was a real Hook event. Saving the same query again updates its label instead of creating duplicates.
- Authored Scene cues belong to the Scene detail sheet in `记忆`, not to a separate primary tab and not to an editor embedded inside Recall Simulation results. The detail sheet must show the existing cues and allow add/edit/delete through `edit_scene` with the current `updated_at`; a conflict must stop rather than overwrite. Recall Simulation may show which authored cue matched and link back to that Scene's memory card. Never synthesize cues from tags, Word Map, title, or body text.
- Show the stored canonical domain in the Scene detail metadata. For a live Ombre projection, allow one Scene's domain to be changed through the authenticated local bridge and the existing Dashboard metadata contract, with an explicit confirmation that saving changes live Ombre. Snapshot-only Scenes stay read-only. Never infer or rewrite a domain from tags, title, Word Map, or prose.
- `例句维护` must cover recall, no-recall, boundary, and historical false-positive examples. Local prototype edits are review drafts only. A future publish action must validate the complete source, increment its dataset version, atomically rebuild every Router vector with the configured embedding profile, and activate it only after successful validation; never edit the live tracked JSON or index directly from the browser.
- Keep Route Example `role` and `origin` as separate axes. Roles are only `典型例句` and `边界例句`; origins include `人工添加`, `线上误召`, `线上漏召`, and `导入`. A historical false recall is an origin, not an example role. Typical examples may describe a route center; boundary examples must not be published as equal-weight route-center embeddings until the backend has an explicit weighting or regression-sample contract.
- Keep `召回观察` separate from both `召回模拟` and `例句维护`. It is a read-only view of real Gateway decisions and actual injections, defaulting to injected records with no-match and skip filters available. Human verdicts stay local; converting an observed false recall or missed recall creates only a local example draft for later review and must never change the live Router, rebuild vectors, or create a formal Gateway injection record.
- The `应属路线` selector in `召回观察` must use the complete local Route Example draft, including newly created unpublished categories, so a reviewed observation can be added to that same draft before publication.
- On the private VPS, keep the shared Route Example review draft in a root-owned server file rather than treating browser `localStorage` as canonical. Migrate an existing browser-only draft on first load, retain localStorage only as an offline mirror, and require the separate confirmed `发布到生产` action before any Router vector rebuild or activation.
- In `召回观察`, keep `Gateway 准备注入` and `Hook 实际送入` visibly separate. Gateway debug says what retrieval prepared; the Haven Bridge UserPromptSubmit hook ledger is the authority for what entered model context. Read only the whitelisted user sentence, message/time IDs, route, trigger, hook outcome, injected memory IDs, compact titles, and scores. Never expose or persist full developer context, `additional_context`, or private rendered prompt text in the prototype.
- In `召回观察`, show actually injected Event and Scene cards inside the existing `真正送入模型` list, labeled by object type. Do not add an `Event / Scene 预计` filter or a separate typed-shadow result panel; candidates and counterfactual previews belong to `召回模拟`, not the real Hook ledger.
- Typed Event/Scene live recall uses a session-wide one-shot boundary: once an `event:` or `scene:` ID has actually entered model context in a session, suppress that same ID for the rest of the session. Apply this after selecting the best cards and do not backfill the freed slot with a lower-ranked candidate; a new session may recall it again.
- Bound the shared injected-bucket ledger: after a write leaves at least 300 injected rows across at least 3 sessions, keep the session that triggered the write and delete injected rows belonging to earlier sessions.
- Keep whole-turn recall verdicts separate from per-memory usefulness in `召回观察`. Only Hook cards that actually entered model context may be labeled `核心相关`, `弱相关`, or `无关`; Gateway prepared candidates must not be presented as delivered-memory judgments. Persist these local candidate judgments by stable memory ID and export only ID, rank, score, and relevance, never the injected body.
- Keep the whole-turn recall action verdict separate from the semantic Route verdict. `正确`/`误召`/`漏召`/`不确定` judge whether the final recall or skip action was right; an optional `路线判断` records `正确`/`错误`/`不确定`, and a wrong route requires an expected Route. Recall/skip classifier labels must use only the action verdict, while Route training may use the separate Route fields.
- When the Hook ledger has no injected IDs and its recorded semantic route currently has the `skip` action, present the outcome as `已跳过` even if the transport-level Gateway trigger is the older `no_match` label. An injected record always remains `已注入`; never rewrite historical outcomes merely because the route action later changed.
- The private VPS prototype is served under `/serein/` as an independent loopback-only Node/Vite service behind HTTPS reverse-proxy authentication. Keep browser credentials out of the bundle; backend bridge secrets stay in a root-readable environment file. Use `OMBRE_LOCAL_COMPOSE_FILE` for local read-only Ombre projections instead of SSHing from the Ombre host back into itself.
- Place `主域边界` between Recall Simulation and Route Example Maintenance. Each canonical Scene domain can be `正常召回`, `仅明确召回`, or `完全排除`, and each domain can be restored independently. Policies read only the Scene's stored `canonical_domain`; never infer a domain from tags, Word Map, legacy bucket labels, title, or prose, and never mutate Scene data when changing a policy. Main-domain edits remain drafts until one explicit confirmed `应用到生产` action publishes the complete versioned policy set; use optimistic version checks and atomically switch only after full validation. Show `domain_explicit_only` and `domain_excluded` in simulator rejection reasons.
- Keep `修订箱` in the Basement tool index. Group revision hints by Narrative Roll and show the exact source type, date, excerpt, matched authored cue/reviewed anchor, source hash, and baseline revision. These derived matches are review hints rather than evidence. Saving a delta creates only an author draft; it must never rewrite or publish a Narrative Roll automatically, and final publication still requires rereading the complete source and current roll.
- Keep `关系提案` in the Basement tool index. Show both Scene titles/dates, relation type, confidence, verbatim evidence on each side, content hashes, model, status, and `review_state`. Only `ready` proposals may be accepted, and accept/reject both require explicit user confirmation. Accept must revalidate both active Scenes, hashes, and quoted evidence before writing a reviewed edge; reject writes no formal edge and changes no Scene. Never present embedding similarity as a formal Scene relation.
- In Basement Relationship Proposals, clicking either Scene opens a compact read-only Scene card over the current proposal list. Re-read that Scene from the canonical endpoint, preserve Markdown projection, close on the veil, close button, or Escape, and keep the reviewer in Basement; do not navigate to the Memory page for ordinary inspection.
